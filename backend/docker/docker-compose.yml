version: "3.8"

services:
  server:
    build:
      context: .
      dockerfile: Dockerfile.dev
    environment:
      NODE_ENV: development
    volumes:
      - ./:/app/
    ports:
      - "3000:3000"
    command: npm run dev